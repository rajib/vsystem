<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html>
  <head>
    <title><%= h(yield(:title) || "Variant System") %></title>
    <%= stylesheet_link_tag 'admin' %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="header" style="padding-left:120px;"> <h1>VARIANT SYSTEM ADMIN PANEL</h1></div>
    <div id="container">
      <%- flash.each do |name, msg| -%>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <%- end -%>
      
      <%- if show_title? -%>
        <h1><%=h yield(:title) %></h1>
      <%- end -%>

       <% if logged_in? %>
         Welcome <%=h current_user.username %>
         <%= link_to "Log out", logout_path %> | <%= link_to "Admin Home Page", base_url %>
       <% end %>
      
      <%= yield %>
    </div>
  </body>
</html>
